openapi: 3.0.0
info:
  title: "Psychologic API"
  description: "API para la aplicación Psychologic"
  version: "0.0.1"

servers:
  - url: "http://localhost:42700"
    description: "Servidor local de desarrollo"

paths:
  /login:
    $ref: 'login.yaml'
  /pacientes:
    $ref: 'pacientes.yaml'
  /pacientes/{id}:
    $ref: 'paciente_id.yaml'
  /psicologos:
    $ref: 'psicologos.yaml'
  /psicologos/{id}:
    $ref: 'psicologo_id.yaml'
